#include <stdio.h>

// Driver code
int main()
{
    float val = 12.3;

    printf("Using %%f: %f\n", val); // show float type
    printf("Using %%e: %e\n", val); // show scientific notation of floats
    printf("Using %%E: %E\n", val); // show scientific notation of floats
    return 0;
}